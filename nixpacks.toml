# Remotion bundles its own ffmpeg and chromium — no extra system packages needed.
# Use npm install instead of npm ci to avoid lock file version mismatch across Node versions.

[phases.install]
cmds = ["npm install"]
